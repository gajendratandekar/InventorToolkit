
<Window x:Class="InventorToolkit.FileManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Inventor File Manager"
        Height="380"
        Width="547"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Import Section"
                   FontSize="16"
                   FontWeight="Bold"
                   Margin="0,0,0,10"/>

        <StackPanel Grid.Row="1" Margin="0,5">
            <Label Content="Import File"/>
            <DockPanel>
                <TextBox Name="TxtImportPath" Width="330" Margin="0,0,10,0"/>
                <Button Content="Browse" Width="100" Click="BrowseImport_Click"/>
            </DockPanel>

            <Button Content="Open In Inventor"
                    Width="150"
                    Margin="0,10,0,0"
                    Click="OpenImport_Click"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="0,20,0,0">
            <Label Content="Export File"/>

            <TextBox Name="TxtExportPath"
             Width="470"
             IsReadOnly="True"
             Margin="0,0,0,10"/>

            <Button Content="Export File"
            Width="150"
            Click="Export_Click"/>
        </StackPanel>

        <StackPanel Grid.Row="3" Margin="0,20,0,0">
            <Button Content="Clear"
                    Width="100"
                    Click="Clear_Click"/>
        </StackPanel>

        <TextBox Name="TxtStatus"
                 Grid.Row="4"
                 Margin="0,20,0,0"
                 IsReadOnly="True"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"/>
    </Grid>
</Window>